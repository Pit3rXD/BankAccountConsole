<Application x:Class="WpfBankAccount.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:WpfBankAccount.Views"
             xmlns:vm="clr-namespace:WpfBankAccount.ViewModels"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:MenuViewModel}">
            <views:MenuView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CheckBalanceViewModel}">
            <views:CheckBalanceView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:DepositViewModel}">
            <views:DepositView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:WithdrawalViewModel}">
            <views:WithdrawalView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:HistoryViewModel}">
            <views:HistoryView/>
        </DataTemplate>
        
    </Application.Resources>
</Application>
