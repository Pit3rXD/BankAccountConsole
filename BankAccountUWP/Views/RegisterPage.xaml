<Page
    x:Class="BankAccountUWP.Views.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BankAccountUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel  HorizontalAlignment="Center" VerticalAlignment="Top" Width="300" Spacing="20">
            <TextBlock Text="Register" FontSize="28"  FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBox PlaceholderText="Owner name" x:Name="OwnerTextBox"/>
            <TextBox PlaceholderText="User  name" x:Name="UserNameTextBox"/>
            <PasswordBox PlaceholderText="Password" x:Name="PasswordBox"/>
            <PasswordBox PlaceholderText="Repeat password" x:Name="RepeatPasswordBox"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0" Spacing="8">
                <Button Content="Register" Margin="0,12,0,0" x:Name="RegisterButton"/>
                <Button Content="Return" Margin="0,12,0,0" x:Name="ReturnButton"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
