<Page
    x:Class="BankAccountUWP.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BankAccountUWP.Views"
    xmlns:viewModels="using:BankAccountUWP.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance viewModels:LoginPageViewModel}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Width="300" Spacing="12">
            <TextBlock Text="Login" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBox PlaceholderText="Username" x:Name="UsernameTextBox" Text="{Binding Username, Mode=TwoWay}"/>
            <PasswordBox PlaceholderText="Password" x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0" Spacing="8">
                <Button Content="Login" Margin=" 0,12,0,0" x:Name="LoginButton" Command="{Binding Path= LoginClicked, Mode=TwoWay}"/>
                <Button Content="Register" Margin="0,12,0,0" x:Name="RegisterButton" Command="{Binding Path= RegisterClicked, Mode=TwoWay}"/>
            </StackPanel>
            <Button Content="Return" Click="BackButton_Click"/>
            <TextBlock x:Name="ErrorTextBlock" Foreground="Red" Text="{Binding ErrorMessage}" TextWrapping="Wrap" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Page>
